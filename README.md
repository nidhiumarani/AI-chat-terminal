# AI Chat Terminal

A terminal based AI chatbot built in Python using Groq API and LLaMA 3.1 model.

## Features
- Chat with an AI model directly from your terminal
- Continuous conversation until you type EXIT
- API key stored safely using .env file

## How to run
pip3 install groq python-dotenv
python3 chat.py